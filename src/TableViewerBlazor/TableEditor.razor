@typeparam T

<MudForm @ref="form"
    IsValid="@success"
    IsValidChanged="OnValidChanged"
    @bind-Errors="@errors"
>
    <TeElementEditor
        Data="Data"
        DataChanged="@(async value => await OnDataChanged(value))"
        Options="Options"
        Depth="1"
        Path="@string.Empty"
    />
</MudForm>
