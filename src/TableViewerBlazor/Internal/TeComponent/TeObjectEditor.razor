@inherits TeEditorBase

@foreach (var keyData in GetKeys(Data!))
{
    var childPath = $"{Path}.{keyData.Key}";
    <TeElementEditor
        Data="keyData.Value"
        DataChanged="@(value => OnDataChanged(keyData.MemberInfo, value))"
        Options="Options"
        Depth="@(Depth + 1)"
        Path="@childPath"
        MemberInfo="keyData.MemberInfo"
        Label="@keyData.Key"
    />
}